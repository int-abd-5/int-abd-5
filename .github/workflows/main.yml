            - name: X Actions Profile
  # You may pin to the exact commit or the version.
  # uses: XERA-2011/x-actions-profile@1098f1530dbdf21be4b7f47232ca4fd2f681201a
  uses: XERA-2011/x-actions-profile@v0.0.1
  with:
    # GitHub Token
    token: 
    # GitHub Username
    user: 
    # Directory to output SVGs
    output_dir: # optional, default is dist
    # Comma separated list of charts to generate: snake,stats,top-langs
    types: # optional, default is snake,stats,top-langs
          
